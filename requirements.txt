# API Gateway & Web UI
fastapi>=0.100.0
uvicorn>=0.22.0
streamlit>=1.25.0
requests>=2.31.0

# Routing & ML
torch>=2.0.0
transformers>=4.30.0
lightgbm>=4.0.0
scikit-learn>=1.3.0
pandas>=2.0.0
numpy>=1.24.0

# Inference Core
# vllm>=0.4.0 (Temporarily disabled for Windows native)
# sglang>=0.1.0 (Temporarily disabled for Windows native)
redis>=5.0.0 # for LMCache mockup

# Orchestration
ray[serve,default]>=2.9.0

# MLOps & Observability
mlflow>=2.5.0
evidently>=0.4.0
prometheus_client>=0.17.0
